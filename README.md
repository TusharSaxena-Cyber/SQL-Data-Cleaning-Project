# SQL-Data-Cleaning-Project
SQL-based data cleaning pipeline for a global layoffs dataset, using staging tables, window functions, and updates to remove duplicates, standardize fields, handle nulls, and produce a clean table ready for analysis.
